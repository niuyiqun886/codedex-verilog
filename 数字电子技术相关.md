
### 必读

|节|页|说明|
|---|---|---|
|1.4.2 反码补码|9–12|别跳。你后面写 CIC/FIR 全是有符号数,Verilog 的 signed 是经典 bug 源|
|5.2 SR锁存器|216|3 页,理解"反馈产生记忆"|
|5.3 电平触发的触发器|219|**这就是锁存器**,你的头号缺口|
|5.5 边沿触发的触发器|230|**这就是 DFF**,和 5.3 对照着读|
|5.6.1 触发器分类|236|只看 D 触发器,JK/T/SR 扫一眼|
|5.7 触发器的动态特性|241|建立/保持时间在这里。**只读参数定义,内部电路分析跳过**|
|6.1 概述|259|Mealy 和 Moore 的区别|
|6.2.1 + 6.2.2|262–270|状态转换图/表,状态机地基|
|6.3.1 寄存器和移位寄存器|272|6 页|
|6.4.1 同步时序设计方法|314|状态机设计流程,全书最实用的一节|
|6.4.2 自启动设计|326|状态机误入非法状态能否自己爬回来,做芯片必须考虑|

### 略读

- **2.5.2 逻辑函数的表示方法**——建立真值表↔电路的对应感,不用刷题
- **2.6.2 卡诺图**——知道综合器在干什么就行
- **4.2 分析与设计方法**、**4.3.2 译码器**、**4.3.3 数据选择器**——只看功能不看门级。数据选择器多看一眼,你写的 `case` 综合出来就是它
- **4.4.1 竞争-冒险成因** + **6.5**——看完就明白同步设计范式在解决什么问题
- **1.5 编码**——只挑格雷码,BCD 跳过
- **7.3.1 SRAM**、**7.5 用存储器实现组合逻辑函数**——后者是 LUT 思想的来源

### 整章跳过

**第三章**(门电路)、**第十章**(脉冲波形)、**第十一章**(数模转换)。

第十一章特别说一句:那些权电阻网络、倒 T 形网络的内容,你做 SAR ADC 的水平远在这本书之上,看了纯属浪费时间。

**第八章**基本全跳。PAL/GAL/EPLD/ispGDS 是九十年代的东西,8.7 的 FPGA 描述也已经严重过时。

**第九章"硬件描述语言简介"——这一章你千万别拿来学 Verilog。** 正文不到十页,写法停留在二十年前,很容易学到一堆坏习惯(比如不分组合/时序块、乱用阻塞赋值)。真要学就 HDLBits。

### 必须背下来的(就这七条)

1. **锁存器电平敏感,触发器边沿敏感**;组合 `always` 块里漏赋值 → 综合出锁存器
2. **T_clk > T_cq + T_组合 + T_setup**——整个数字设计的地基
3. **D 触发器特性方程 Q(n+1) = D**。JK/T/SR 的特性方程不用背,RTL 里你只会用 D
4. **德摩根定律**——写条件判断时天天用
5. **补码三条**:负数怎么表示、符号扩展往高位补符号位、加法结果位宽 +1 / 乘法位宽相加
6. **格雷码相邻码字只变 1 位**——记住这条性质就够,编码规则用到时再查
7. **Mealy 输出跟输入走会有毛刺,Moore 输出只跟状态走更干净**

其余全部理解一次即可,不用记。卡诺图、逻辑代数各种定理、74 系列型号,以后一次都不会用到。

### 三个陷阱

**5.4 脉冲触发的触发器(主从结构)跳过。** 它的"一次变化问题"是主从 JK 特有的历史缺陷,边沿触发出现后就被淘汰了。看了只会增加困惑。

**"异步时序逻辑电路"(6.2.3、6.4.3)不是跨时钟域。** 它指没有统一时钟、靠传播延迟工作的电路,分析要画流态图查竞态,纯考研内容。你以后要处理的 CDC 是完全另一套东西。

**6.3.2 计数器有 31 页,只看前 5 页。** 同步计数器原理看一下,后面 74LS161/74LS90 级联接 N 进制那一大段直接跳——RTL 里就一句 `cnt <= cnt + 1'b1;`。